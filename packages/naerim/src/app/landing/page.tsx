import { css } from '../../../styled-system/css';

/**
 * landing page
 */
export default function LandingPage() {
  return (
    <div
      className={css({
        fontSize: '1.5rem',
        fontWeight: 'bold',
        color: 'GrayText',
      })}
    >
      Landing
    </div>
  );
}
